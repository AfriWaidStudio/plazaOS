import { Text } from '../../components'

// Placeholder only — tenant screens (profile, payments, maintenance,
// announcements, notifications) are built in a later sprint.
export function TenantHome() {
  return (
    <div>
      <Text variant="h1">Tenant home</Text>
      <Text variant="body" className="mt-2 text-slate-500">
        Placeholder — tenant screens land here.
      </Text>
    </div>
  )
}
