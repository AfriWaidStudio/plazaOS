import { Text } from '../../components'

// Placeholder only — admin screens (units, tenants, payments, maintenance,
// announcements, reminders, calendar) are built in a later sprint.
export function AdminHome() {
  return (
    <div>
      <Text variant="h1">Admin dashboard</Text>
      <Text variant="body" className="mt-2 text-slate-500">
        Placeholder — admin screens land here.
      </Text>
    </div>
  )
}
